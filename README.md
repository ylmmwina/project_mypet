# 🐾 MyPet

**MyPet** - це навчальний проєкт, створений у стилі класичних ігор-тамагочі.
Гравець обирає улюбленця та доглядає за ним: годує, грає, лікує, прибирає і стежить за його станом.
Мета гри - підтримувати тваринку щасливою, здоровою та ситою.

Гра матиме простий графічний інтерфейс у **піксель-арт стилі**, з власноруч намальованими спрайтами й базовими анімаціями.

---

**MyPet** поєднує:
-  **Бекенд** на Node.js — логіка гри, стани, збереження.
-  **Фронтенд** на HTML, CSS, JS — інтерфейс у браузері.

---

## ✨ Основні можливості

-  Вибір тваринки: кіт 🐱, пес 🐕, кролик 🐰  
-  Показники стану: `Health`, `Hunger`, `Happiness`, `Energy`  
-  Дії користувача: `Feed`, `Play`, `Sleep`, `Clean`, `Heal`  
-  Збереження стану: у форматі **JSON**  
-  Простий інтерфейс з анімаціями та індикаторами настрою  
-  Мова інтерфейсу: українська 🇺🇦 (планується англійська версія 🇬🇧)

---

## 🧱 Використані технології

| Категорія | Технології |
|------------|-------------|
| **Backend** | Node.js, Express.js, CORS |
| **Frontend** | HTML5, CSS3, JavaScript (ES6) |
| **Testing** | Jest, Supertest |
| **Інше** | Git + GitHub, npm, Live Server |

---

## 🎨 Візуальний стиль

- Власноруч створений **піксель-арт 32×32 px**, без згладжування (nearest-neighbor)
- Затишна ретро-естетика, яскраві кольори, прості анімації
- Усі спрайти, предмети та іконки малюються командою проєкту

---

## 🧪 Тестування (Unit Tests)

У проєкті реалізовано автоматичне тестування для забезпечення якості коду та відповідності вимогам OOP.
Для тестування використовуються фреймворки **Jest** та **Supertest**.

### Покриття тестами:
1.  **Models (`pet.test.js`):** Перевірка створення об'єктів, валідації даних та методів серіалізації (`toJSON`/`fromJSON`).
2.  **Utils (`storage.test.js`):** Перевірка роботи з файловою системою через імітацію (mocking) модулів Node.js, щоб уникнути створення реальних файлів під час тестів.
3.  **API Routes (`routes.test.js`):** Інтеграційне тестування HTTP-запитів (`GET`, `POST`) до бекенду.

### Як запустити тести:

```bash
npm test
```

### ⚠️ Відомі проблеми (Known Issues)

На поточний момент інтеграційні тести маршрутів (`tests/routes.test.js`) завершуються з помилкою (**FAIL**).

* **Статус:** Виявлено баг у файлі `backend/routes/petRoutes.js`.
* **Опис:** Сервер повертає помилку 500 через відсутній імпорт класу `Storage`.
* **Рішення:** Баг задокументовано тестами, виправлення очікується в наступному коміті інших учасників команди.